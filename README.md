# ICOM Repeater List Builder

Tools to parse RepeaterBook CSV exports and generate imported-ready repeater lists for the ICOM ID-50 (and compatible) radios.

## Features
- Extracts city/state data from RepeaterBook CSVs.
- Generates import files with correct latitude/longitude and formatting.

## Usage
(Add specific usage instructions here)
